Let's add support fo OpenRouter provider for LLM model for reflections.

I want to add it alongside Ollama. I want to configure it all in one place, so .env. I want an easy switch in the config where I can have both defined and just toggle between Ollama and OpenRouter, so with another var in .env?

Let's plan this feature carefully and then implement it correctly.